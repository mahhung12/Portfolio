import { USER_TABS } from "./author";
import { PROJECT_STATUS } from "./common";

const { ALL, BLOCKCHAIN, MAIN, OTHERS } = USER_TABS;
const { PENDING, INPROGRESS, RELEASE, DONE } = PROJECT_STATUS;
// const {};

export const AUTHOR_PROJECTS = [
  {
    TYPE: BLOCKCHAIN,
    TITLE: "C2C Whitelabel Marketplace",
    STATUS: INPROGRESS,
    // POSITION:
  },
];
